<policies>
    <inbound>
        <base />
        <choose>
            <when condition="@(context.Deployment.Region == &quot;North Central US&quot;)">
                <set-backend-service base-url="{{backend-url-functions-secondary}}" />
            </when>
            <otherwise>
                <set-backend-service base-url="{{backend-url-functions-primary}}" />
            </otherwise>
        </choose>
        <set-header name="X-Forwarded-Host" exists-action="override">
            <value>@(context.Request.OriginalUrl.Host)</value>
        </set-header>
        <set-header name="X-APIM-Region" exists-action="override">
            <value>@(context.Deployment.Region)</value>
        </set-header>
        <!-- Failover endpoint has stricter rate limit -->
        <choose>
            <when condition="@(context.Operation.Id == "TriggerFailover")">
                <rate-limit-by-key calls="5" renewal-period="60"
                    counter-key="@(context.Subscription?.Key ?? context.Request.IpAddress)" />
            </when>
            <otherwise>
                <rate-limit-by-key calls="200" renewal-period="60"
                    counter-key="@(context.Subscription?.Key ?? context.Request.IpAddress)" />
            </otherwise>
        </choose>
    </inbound>
    <backend>
        <base />
    </backend>
    <outbound>
        <base />
        <set-header name="X-Powered-By" exists-action="delete" />
        <set-header name="X-AspNet-Version" exists-action="delete" />
    </outbound>
    <on-error>
        <base />
        <set-header name="X-Error-Source" exists-action="override">
            <value>APIM-Status-API</value>
        </set-header>
    </on-error>
</policies>
